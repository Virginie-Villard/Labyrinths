/*
//start
let position; // = findEntrance();


if(cell.walls) {
    // Ne peut jamais traverser
}
// ____________________________________

if(cell.walls.length === 3) {
    // avance sur la case sans mur
}
// ____________________________________

if(cell.walls.length === 2) {
    // choose(the only other way)
}
// ____________________________________

if(cell.walls.length > 2) {
    // choose(way)
}
// ____________________________________


if(cell.walls.length === 2) {
    // avance sur la case de laquelle tu ne viens pas
}

if(position === cell.exit) {
    // "Win !"
}


// _Functions______________________________________________________________

function findEntrance() {
    for(i in cells) {
        return cell.entrance = true;
    }
}

// ____________________________________
function myNeighbours(position) {
    if(cell.walls[0]) {
        goTop = false
    }

    if(cell.walls[1]) {
        goRight = false
    }

    if(cell.walls[2]) {
        goBottom = false
    }

    if(cell.walls[3]) {
        goLeft = false
    }
}

// ____________________________________
function goRight(position) {
    position = div[i + 1];
}
function goDown(position) {
    position = div[i + size];
}
function goTop(position) {
    position = div[i - size];
}
function goLeft(position) {
    position = div[i - 1];
}
// ____________________________________


function choose(way) {

    let n; // n = nombre de possibilités; (entre 1 et 4)

    // if(n > 2) {
    //     // Math.floor(n) * Math.random();
    // }

    // choisis gauche.

}

function isVisited(cell) {
    let cell.visited = 0;

    if(cell.visited) {
        visited ++;
    }

    return visited;
}

// ________________________________________________________________________







*/